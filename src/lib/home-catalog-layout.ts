/** Horizontal inset for homepage catalog content (matches Tailwind `px-8`). */
export const HOME_PAGE_GUTTER_CLASS = "px-8";

/** Max-width shell for homepage chrome only (navbar + footer). */
export const HOME_CHROME_MAX_WIDTH_CLASS = "mx-auto w-full max-w-[1440px]";
