/**
 * Full-bleed backgrounds site-wide (hero + all pages). Images rotate every 10 seconds.
 *
 * Files live in `/public` and are referenced with a leading slash (e.g. `/photo.jpg`).
 */
export const HOME_HERO_IMAGES: string[] = [
  "/background-image-01.jpg",
  "/background-image-02.jpg",
];
